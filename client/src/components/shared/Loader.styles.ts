const styles = {
  wrapper: {
    position: 'relative' as const,
    opacity: 0.7,
  },
  progress: {
    position: 'absolute' as const, 
    top: 'calc(50% - 20px)', 
    left: 'calc(50% - 20px)',
  },
};

export default styles;
